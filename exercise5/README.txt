Defined the maximal size of the stack is 1000.

For function top(), pop() and clear(), if the stack is empty, it will throw an exception "The stack is empty.".
For function push(), if the stack is full, it will throw an exception "The stack is full.".